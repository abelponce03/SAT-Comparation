"""
Solver plugins package — auto-discovered by the registry.

To add a new solver, create a new Python file in this directory
(e.g. ``my_solver.py``) and define a class that inherits from
``SolverPlugin``.  It will be picked up automatically on next startup.

See existing plugins for examples.
"""
